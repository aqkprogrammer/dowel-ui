---
"@dowel-ui/docs": patch
---

Show the Display category on the documentation site.

`CATEGORY_ORDER` did not name it, and the grouping filtered to that list, so
Avatar, Badge and Card were absent from the components index and the sidebar
with nothing reporting it. Categories the curated order does not name are now
appended rather than dropped.
